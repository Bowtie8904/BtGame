package bt.game.resource.render.impl;

import javax.swing.ImageIcon;

/**
 * @author &#8904
 *
 */
public class RenderableGif extends RenderableImage
{
    public RenderableGif(ImageIcon image)
    {
        super(image.getImage());
    }
}